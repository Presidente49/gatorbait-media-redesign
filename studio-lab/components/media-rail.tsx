"use client";
import useEmblaCarousel from "embla-carousel-react";import {ChevronLeft,ChevronRight,Play} from "lucide-react";
export function MediaRail(){const [ref,api]=useEmblaCarousel({align:"start",dragFree:true});const shows=["The Buddy Martin Show","Florida Gator Lowdown","Best Friday in Football","Latest Clips","From the Archive"];return <div className="railWrap"><div className="railControls"><button aria-label="Previous" onClick={()=>api?.scrollPrev()}><ChevronLeft/></button><button aria-label="Next" onClick={()=>api?.scrollNext()}><ChevronRight/></button></div><div className="embla" ref={ref}><div className="emblaTrack">{shows.map((x,i)=><article className="mediaTile" key={x}><Play/><div className="k">{i===0?"LATEST":"GATORBAIT TV"}</div><h3>{x}</h3><p>Watch · Save · Listen</p></article>)}</div></div></div>}
